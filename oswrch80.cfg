MEMORY
{
   RAM1:  start = $78ea, size = $16, fill = no;
   RAM2:  start = $7900, size = $700, fill = no;
}

SEGMENTS
{
   HEADER:  load = RAM1, type = ro, optional = yes;
   CODE:    load = RAM2, type = ro;
}

