MEMORY
{
   RAM1:  start = $77ea, size = $16, fill = no;
   RAM2:  start = $7800, size = $800, fill = no;
}

SEGMENTS
{
   HEADER:  load = RAM1, type = ro, optional = yes;
   CODE:    load = RAM2, type = ro;
}
